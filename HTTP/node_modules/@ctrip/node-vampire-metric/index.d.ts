interface opts {
    namespace?: string,
    name: string,
    tags?: Object,
    interval?: number
}

export default class Metric {
    constructor(opts: opts);

    add(value: number, tags: Object): void;

    start(): void;

    clear(): void;

    end(): void;
}