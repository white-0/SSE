import { RouteServerList, RouteContext } from '../utils/utils';
export declare class CheckRouteRules {
    private subEnv;
    private zoneId;
    private region;
    routeContext: Array<RouteContext>;
    constructor(opt: any);
    getAvailableServers(lookUpServices: any, ucs_strategy: any): void;
    private _getAvailableServers;
    /**
     * 匹配路由规则
     * @param routeContext
     * @param operationName
     * @param routeId
     * @param isUcs
     * @returns
     */
    static reRoute(routeContext: Array<RouteContext>, operationName: string, routeId?: string, isUcs?: boolean): Array<RouteServerList>;
    static tryMatch(values: any): boolean;
    /**
     *
     * @param instances {Array}
     * @returns Generate a default route
     */
    private generateInstance;
}
