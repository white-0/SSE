import { CallerType } from './utils';
/**
 * 获取Artemis 服务器列表
*/
export declare class ArtemisServerList {
    private operationType;
    private _retryGetNodes;
    private clientType;
    artemisPromise: any;
    private listNodes;
    constructor(operationType: string, clientType: CallerType);
    getNodes(): Promise<any>;
    private _getNodes;
    private _intervalGetNodes;
}
