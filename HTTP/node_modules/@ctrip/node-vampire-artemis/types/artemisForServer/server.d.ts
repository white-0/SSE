import { CallerType } from '../utils/utils';
interface SoaServiceConfig {
    AppID: string;
    ServiceId: string;
    ServerPort?: number;
    vd: string;
    Env: string;
    frameworkVersion: string;
}
export declare class Server {
    private ServerIp;
    private ServerPort;
    private host;
    private RegionId;
    private HealthCheckUrl;
    private ZoneId;
    private StartUpTime;
    config: SoaServiceConfig;
    private clientType;
    constructor(clientType?: CallerType);
    /**
     * artemis register
     * @param c {
        AppID: string,
        ServiceId: string,
        ServerPort?: number,
        vd: string,
        Env: string,
        frameworkVersion: string
    }  Service Registration Parameters
     */
    initConfig(c: any): void;
    private _getInstance;
    /**
     * send heartBeat
     * @returns Promise<void>
     */
    heartBeat(): Promise<void>;
    /**
     * Send heartbeat every 5s
     * @returns Promise<void>
     */
    private _intervalHeartBeat;
    private _setHeartBeat;
}
export {};
