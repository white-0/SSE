const assert = require('assert');

const pkgInfo = require('../index');

describe('Show pkgInfo', ()=>{
    it('pkginfo list', (done)=>{
        pkgInfo.getInfo({}, (data) => {
            assert.ok(1);
            console.log(data);
            done();
        });
    })
});