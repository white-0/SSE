'use strict'
const pkg = require('../../package')

module.exports = {
  /**
   * The version of Cargo SDK.
   */
  SDK_VERSION: pkg.version,

  /**
   * The default appId.
   */
  DEFAULT_APPID: '999999999',

  /**
   * Node.js SDK
   */
  DATA_SOURCE: 3,

  /**
   * The separator of site's hosts and channel's alias.
   */
  SEPARATOR: ',',

  /**
   * The internals keys in data cache
   */
  INTERNAL_BOOLEAN_KEYS: {
    /**
     * The global manual currency switches
     */
    CURRENCY_SWITCH: 'CurrencySwitch',

    /**
     * The global supported locale for online
     */
    ONLINE_SUPPORTED_LOCALE: 'SupportedLocale',

    /**
     * The global supported locale for h5
     */
    H5_SUPPORTED_LOCALE: 'H5SupportedLocale',

    /**
     * The top currencies group in boolean value
     */
    TOP_CURRENCY: 'TopCurrency'
  }
}
