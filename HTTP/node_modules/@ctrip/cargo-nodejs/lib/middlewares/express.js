'use strict'
const cargo = require('../cargo')

module.exports = contextType => (req, res, next) => {
  const cargoContext = cargo.getCargoContext(req, res, contextType)
  cargoContext.setBrowserCookies()
  req.cargoContext = cargoContext
  req.cargo = cargoContext // 兼容旧代码
  next()
}
