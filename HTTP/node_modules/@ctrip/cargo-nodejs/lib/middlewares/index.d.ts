import * as Express from 'express';
import * as Koa from 'koa';
import { CONTEXT_TYPE } from '../../index';

export const express: (contextType?: CONTEXT_TYPE) => (request: Express.Request, response: Express.Response, next: Express.NextFunction) => void;
export const koa: (contextType?: CONTEXT_TYPE) => Koa.Middleware<any, {}>;
