export declare type QueryObject = Record<string, string | number | undefined>;
/**
 * 把对象转化成 search query
 * @param ignoreNull 忽略值为 null or undefined 的键
 */
export declare const buildQuery: (query?: QueryObject, ignoreNull?: boolean, encoder?: (item: string) => string) => string;
/**
 * 把 search query 转换成对象
 * @param query
 * @param overrideSameKey 当有多个相同的 key 是，是否 override, 如果不 override, 则会组成一个数组
 * @example parseQuery('id=1&id=2', false) => { id: [1, 2] }
 */
export declare function parseQuery(query: string, overrideSameKey: true, decoder?: (val: string) => string): Record<string | number, string>;
export declare function parseQuery(query: string, overrideSameKey?: false, decoder?: (val: string) => string): Record<string | number, string | string[]>;
