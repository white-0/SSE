import { Context } from "./context";
import { BaseScope } from './BaseScope';
/**
 * 应用 Heartbeat & ThreadDump 指标，可在BAT/CAT 平台查看
 */
export declare class BatHeartbeat extends BaseScope {
    context: Context;
    __type: string;
    status: string;
    sendBytes: number;
    constructor(sendBytes: number);
    heartbeat(): Promise<void>;
    threadDump(event: any): Promise<Array<void>>;
}
