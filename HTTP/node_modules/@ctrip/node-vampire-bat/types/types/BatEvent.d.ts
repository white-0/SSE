import { BaseScope } from './BaseScope';
export declare class BatEvent extends BaseScope {
    __type: string;
    status: string;
    constructor(...args: [type: string, name: string, data?: any]);
    beforeSend(): void;
    private createError;
}
