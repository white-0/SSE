export declare class TraceContext {
    context: any;
    TraceContextOP: string;
    markerId: number;
    constructor();
    increaseMarkerId(): void;
    restoreTraceContext(headers: object): void;
    restore(objs: object): void;
    remove(key: string): string;
    containsKey(key: string): boolean;
    getContext(): object;
    get(key: string): string;
    addAll(objs: object): void;
    add(key: string, value: string): string;
    toString(): string;
}
