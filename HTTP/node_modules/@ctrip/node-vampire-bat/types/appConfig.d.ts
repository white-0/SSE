declare class catConfig {
    private cat_server_host_default;
    debug: boolean;
    Env: string;
    private initPromise;
    AppID: string;
    IDC: string;
    Zone: string;
    constructor();
    setDefaultConfig(key: string, value: any): void;
    updateCatServerConfig(): void;
    init(): Promise<void>;
}
export declare const appConfig: catConfig;
export {};
