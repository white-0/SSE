"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.PhoneFormatter = void 0;
var base_1 = require("../base");
var PhoneFormatter = (function (_super) {
    __extends(PhoneFormatter, _super);
    function PhoneFormatter(i18n) {
        return _super.call(this, i18n) || this;
    }
    PhoneFormatter.prototype.validatePhoneNumber = function (regionCode, phoneNumber) {
        var validLengthList = (this.get("key.l10n.phonenumber.length.".concat(regionCode)) || '')
            .split(',')
            .filter(function (n) { return /^\d+$/.test(n); })
            .map(function (n) { return parseInt(n, 10); });
        if (validLengthList.length) {
            return validLengthList.find(function (n) { return phoneNumber.toString().length === n; }) !== undefined;
        }
        return true;
    };
    return PhoneFormatter;
}(base_1.Formatter));
exports.PhoneFormatter = PhoneFormatter;
