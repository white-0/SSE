import { Formatter } from '../base';
import { II18n } from '../types';
export declare class DurationFormatter extends Formatter {
    private readonly locale;
    constructor(i18n: II18n, locale?: string);
    convertToString(duration: number, pattern: string, isAutoAbbr?: boolean, isFullName?: boolean): string;
    private getByTemplate;
    private unitFromFlag;
    private calculateValue;
    private autoAbbrUnits;
    private matchNoneZero;
    private formatString;
    private formatFullNameString;
    private formatShortNameString;
}
