export type IRule = (integer: number, fraction: number, fractionBitsCount: number) => boolean;
export type PluralType = {
    ZERO: string;
    ONE: string;
    TWO: string;
    FEW: string;
    MANY: string;
    OTHER: string;
};
export type RuleMapping = {
    [key: string]: [string, IRule][];
};
export declare const RULE_MAPPING: RuleMapping;
