import { Timezone } from '../types';
export declare const TIMEZONE_OFFSET: number;
export declare const convertTimezoneOffset: (timezone?: Timezone) => number;
export declare const convertFormat: (format: string) => string;
