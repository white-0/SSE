import { IRule } from './mappping';
export declare class PluralRule {
    private readonly rule;
    constructor(locale: string);
    private normalizeLocale;
    get size(): number;
    getSuffix(index: number): string;
    canMatch(index: number): IRule;
}
