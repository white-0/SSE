export var RU = function (integer, fraction, fractionBitsCount) {
    if (fractionBitsCount !== 0) {
        return false;
    }
    var mod10 = integer % 10;
    var mod100 = integer % 100;
    return mod10 === 0 || mod10 >= 5 || (mod100 >= 11 && mod100 <= 14);
};
export var PL = function (integer, fraction, fractionBitsCount) {
    if (fractionBitsCount !== 0) {
        return false;
    }
    var mod10 = integer % 10;
    var mod100 = integer % 100;
    return (integer !== 1 && (mod10 >= 0 && mod10 <= 1)) ||
        (mod10 >= 5 && mod10 <= 9) ||
        (mod100 >= 12 && mod100 <= 14);
};
export var UK = function (integer, fraction, fractionBitsCount) {
    if (fractionBitsCount !== 0) {
        return false;
    }
    var mod10 = integer % 10;
    var mod100 = integer % 100;
    return mod10 === 0 ||
        (mod10 >= 5 && mod10 <= 9) ||
        (mod100 >= 11 && mod100 <= 14);
};
export var AR = function (integer, fraction, fractionBitsCount) {
    if (fraction !== 0) {
        return false;
    }
    return integer % 100 >= 11;
};
