var DurationHourUnit = (function () {
    function DurationHourUnit() {
        this.fullSharkKey = 'key.duration.hour';
        this.shortSharkKey = 'key.duration.h';
        this.display = true;
        this.value = 0;
    }
    DurationHourUnit.prototype.decimalTransform = function (second) {
        this.value = parseInt(String((second / 3600)));
    };
    DurationHourUnit.prototype.transform = function (second) {
        this.value = parseInt(String((second % 86400) / 3600));
    };
    return DurationHourUnit;
}());
export { DurationHourUnit };
