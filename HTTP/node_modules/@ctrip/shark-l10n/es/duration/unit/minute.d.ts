import { DurationUnit } from './interface';
export declare class DurationMinuteUnit implements DurationUnit {
    fullSharkKey: string;
    shortSharkKey: string;
    display: boolean;
    value: number;
    decimalTransform(second: number): void;
    transform(second: number): void;
}
