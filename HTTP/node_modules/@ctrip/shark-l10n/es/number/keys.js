export var getCurrencyKey = function (key, currency) {
    return currency ? "".concat(key, ".").concat(currency) : key;
};
export var getSpecialCurrencyKey = function (key, currency) {
    return currency ? "".concat(key, ".").concat(currency, ".special") : key;
};
