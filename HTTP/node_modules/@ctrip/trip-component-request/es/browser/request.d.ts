import { RequestOptions } from '../interface';
import { RequestError } from '../shared/errors';
declare global {
    interface Window {
        [key: string]: any;
    }
}
interface AjaxResult<T = any> {
    response: T;
    url: string;
    xhr: XMLHttpRequest;
}
interface AjaxOptions extends RequestOptions {
    __success?: (result: AjaxResult, xhr: XMLHttpRequest) => void;
    __error?: (err: RequestError, xhr: XMLHttpRequest) => void;
}
export declare function _ajax(options: AjaxOptions): XMLHttpRequest;
declare const request: import("../interface").RequestStatic;
export { request };
