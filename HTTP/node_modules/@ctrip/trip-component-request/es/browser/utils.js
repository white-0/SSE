export function transformEventToError(errOrEvent) {
  if (errOrEvent instanceof Error) {
    return errOrEvent;
  }
  var error = new Error('');
  error.event = errOrEvent;
  return error;
}
export var parseXOriginUrlToAkRegion = function parseXOriginUrlToAkRegion(xOriginUrl) {
  if (!xOriginUrl) {
    return undefined;
  }
  try {
    var akRegion = new URL(xOriginUrl).searchParams.get('region');
    return akRegion || '';
  } catch (e) {}
  return undefined;
};