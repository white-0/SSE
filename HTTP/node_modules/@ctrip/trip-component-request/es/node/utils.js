export function lazyRequire(moduleName) {
  var val;
  return {
    get current() {
      if (!val) {
        try {
          // avoid webpack require this module
          val = eval('require')(moduleName);
        } catch (e) {
          // don't throw error,
        }
      }
      return val;
    }
  };
}