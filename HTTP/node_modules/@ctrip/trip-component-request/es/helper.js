export var bindRequestMethods = function bindRequestMethods(req) {
  var request = req;
  request.get = request.get || function (url) {
    var opt = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
    opt.method = 'GET';
    opt.url = url;
    return request(opt);
  };
  request.post = request.post || function (url, data) {
    var opt = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
    opt.method = 'POST';
    opt.url = url;
    opt.data = data;
    return request(opt);
  };
  request.put = request.put || function (url, data) {
    var opt = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
    opt.method = 'PUT';
    opt.url = url;
    opt.data = data;
    return request(opt);
  };
  return request;
};