import { AbortablePromise } from '../interface';
export { buildQuery, parseQuery } from '@ctrip/trip-component-utils';
export declare type QueryObject = Record<string, string | number | undefined>;
/**
 * 返回一个新的 promise, 在 time 内如果原来的 promise 没有 resolve，则 reject;
 * @param time 时间
 * @param promise promise
 */
export declare function promiseTimeout<T>(time: number, promise: Promise<T>): Promise<T>;
export declare function promiseAbort<T>(promise: Promise<T>): AbortablePromise<T>;
export declare function isAbortable<T>(promise: Promise<T>): promise is AbortablePromise<T>;
export declare function validStatus(status: number, protocol?: 'file:' | 'http:' | 'https:'): boolean;
