import { RequestOptions } from '../interface';
export declare function isEqual(valA: any, valB: any): boolean;
export declare class CacheManager {
    private maxSize;
    private cacheList;
    constructor();
    exist(opt: RequestOptions): boolean;
    get(opt: RequestOptions): Promise<any> | undefined;
    add(opt: RequestOptions, promise: Promise<any>): void;
}
