import clogging from '../';
clogging.setCollectStdio(true);
clogging.custom({
    type: 'error',
    title: '',
    message: 'err',
    addInfo: {
        'test': 'just a clogging test',
        'aa': {'xx': 1, 'yy': 2}
    }
});
clogging.info('test');
console.log('test clogging setCollectStdio===');
setInterval(()=>{},4000000)