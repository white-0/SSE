import { SoaClientInitOptions, InvokeOptions } from './utils/soaMonitor';
import { SoaClientBaseApi } from './soaClientBaseApi';
export declare class SoaClientMeshApi extends SoaClientBaseApi {
    private SoaMeshInitCache;
    constructor(opts: SoaClientInitOptions);
    init(): Promise<void>;
    invoke<T>(opts: InvokeOptions<T>): Promise<any>;
    protected childInvoke(opts: {
        method?: string;
        reqUrl: string;
        httpOpt: any;
        requestBody: string;
    }): Promise<any>;
}
