import { SoaClientInitOptions, InvokeOptions } from './utils/soaMonitor';
import { SoaClientBaseApi } from './soaClientBaseApi';
import { Client } from '@ctrip/node-vampire-artemis';
interface SoaDirectInvokeOptions<T> extends InvokeOptions<T> {
    routeId?: string;
    userId?: string;
}
export declare class SoaClientDirectApi extends SoaClientBaseApi {
    protected artemisClient: Client;
    private SoaDirectInitCache;
    private soaServerUrl;
    constructor(opts: SoaClientInitOptions);
    init(): Promise<void>;
    invoke<T>(opts: SoaDirectInvokeOptions<T>): Promise<any>;
    protected childInvoke(opts: {
        method?: string;
        reqUrl: string;
        httpOpt: any;
        requestBody: string;
        opt?: string;
    }): Promise<any>;
}
export {};
